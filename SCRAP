
<% faves.forEach((fave) => { %>
    <p> <%= fave.spotify_id %>  </p>
<% }) %>


<% fave.forEach((faves) => { %>
<% faves.spotify_id %> 
<% }); %> 


<h1>Your favorite songs</h1>
<% tracks.forEach((fave) => { %>
  <% if (fave.spotify_id && fave.userId) {  %> 
  <p> <%= tracks.name %>  </p>
<% } %>
<% }) %>


<h1>My Favorites</h1>
<% faves.forEach((fave) => { %>
  <% fave.spotify_id %> 
  <% }); %> 
  

  <pre><%= JSON.stringify(fave) %></pre>

  <% const isNull = (previewValue) => { %>
    <% return previewValue === null ? %>
    <p>No Preview Available</p> 
    <% : %>
    <audio controls><source src="<%= fave.preview_url %>" /></audio> 
<% } %> 


    <p><%= isNull(fave.preview_url) %> </p>

    const handler = (req, res) => res.send(path.join(__dirname, "/"))

const routes = ["/", ":track"]

routes.forEach( route => app.get(route, handler) 




<h1>My Favorite Tunes</h1>
<% faves.forEach((fave) => { %>
  <p><%= fave.title %> </p>
  <p><%= fave.artist %> </p>
  <p><%= fave.explicit %> </p>
  <p><%= fave.durationMs %> </p>
  <% console.log(fave.preview_url) %>
  <% if (fave.preview_url === "") { %>
      <p>No Preview Available</p> 
  <% } else { %>
      <audio controls><source src="<%= fave.preview_url %>" /></audio> 
  <% } %> 
<% }) %>
